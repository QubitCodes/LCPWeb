"use client";
const Navbar = () => {
  return (
    <nav  
      className="flex bg-sidebar items-center w-full justify-start rounded-2xl"
      style={{ height: `40px`, borderRadius: `24px` }}></nav>
  );
};

export default Navbar;
